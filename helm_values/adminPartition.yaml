global:
  name: frontend
  enabled: false
  image: hashicorp/consul-enterprise:1.21.8-ent
  logLevel: "debug"
  enterpriseLicense:
    secretName: consul-ent-license
    secretKey: key
  peering:
    enabled: true
  adminPartitions:
    enabled: true
    name: servicos-a2
  enableConsulNamespaces: true
  tls:
    enabled: true
    caCert:
      secretName: consul-ca-cert 
      secretKey: tls.crt
    caKey:
      secretName: consul-ca-key 
      secretKey: tls.key
  acls:
    manageSystemACLs: true
    bootstrapToken:
      secretName: consul-partitions-acl-token 
      secretKey: token
# client:
#  enabled: false
#  grpc: true
externalServers:
  enabled: true
  hosts: [a1f75a51abcd4416e8a9203e01d61d3f-2119995519.eu-west-2.elb.amazonaws.com]
  tlsServerName: server.dc1.consul
  k8sAuthMethodHost: https://ADE6F919E3D501F3BC2CA2078CA697D7.gr7.eu-west-2.eks.amazonaws.com
controller: 
  enabled: true
connectInject:
  enabled: true
  default: true                
  transparentProxy:
    defaultEnabled: true         # Default
    logLevel: "debug"
# envoyExtraArgs: "--log-level debug"
metrics:
  defaultEnabled: true
  defaultEnableMerging: true
  apiGateway:
    manageExternalCRDs: true
    # Configuration settings for the GatewayClass
    managedGatewayClass:
      # Defines the type of service created for gateways (e.g. LoadBalancer, ClusterIP, NodePort)
      # LoadBalancer is primarily used for cloud deployments.
      serviceType: LoadBalancer  
terminatingGateways:
  enabled: true
meshGateway:
  enabled: true
  
#  replicas: 1
# service:
#   enabled: true
#   type: 'LoadBalancer'
# wanAddress:
#   source: Service
